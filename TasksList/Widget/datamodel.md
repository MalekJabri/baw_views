## Business Data

The widget expects an array of task items. Each item should be an object with the following structure:

```javascript
    [ 
        {
        "label" : "String",
        "status" : "String"
        }
    ]
```

Status can be: pending, processing, complete, failed
